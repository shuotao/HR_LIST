## [2026-04-29 --:--] session-summary | HRMD 履歷健檢網頁 MVP 建立 + Firebase 部署規劃

- actor: claude-opus-4-6 (via Claude Code CLI)
- trigger: manual (/handoff)
- summary: 完成 HRMD 履歷健康檢查網頁全部原始碼（前端+後端共 12 檔），規劃 Firebase Hosting + Cloud Run 部署架構，前端已可本機預覽。

### 本 session 完成
- 建立 `web/` 子目錄，包含完整前後端共 12 個檔案
- **後端 (web/backend/)**：Flask API + MarkItDown PDF 轉檔 + 欄位擷取 + M/N/E/D 通用評分 + BIM 主任職缺專屬評分
  - `main.py` — Flask POST `/api/analyze` 端點
  - `extractor.py` — 移植自 `extract_hr_data.py` 的欄位擷取邏輯（改為接受字串輸入）
  - `scoring.py` — 移植自 `screen_candidates.py` 的完整 39 條規則評分引擎
  - `bim_scorer.py` — 全新 BIM 主任職缺 100 分制評分（學歷/BIM經驗/英語/工程專業/管理層級）
  - `Dockerfile` + `requirements.txt` — Cloud Run 容器定義
- **前端 (web/public/)**：中文 UI 單頁應用
  - `index.html` — 職缺資訊卡 + PDF 拖曳上傳 + 結果展示（圓形儀表+條狀圖+推薦等級）
  - `style.css` — 藍色系專業風格、RWD 適配、conic-gradient 圓形進度條
  - `app.js` — 前端邏輯（拖曳上傳、API 呼叫、結果渲染）
- **Firebase 設定**：`firebase.json`（Hosting + Cloud Run rewrite）、`.firebaserc`
- Firebase CLI v15.15.0 已透過 npm 安裝完成
- 計畫檔位於 `.claude/plans/optimized-finding-tower.md`

### 對齊的概念
- **架構選擇：Firebase Hosting + Cloud Run（非 Cloud Functions）**：因為 MarkItDown + 依賴超過 500MB，Cloud Functions gen2 有部署大小限制，Cloud Run 無此限制且同樣有 2M 請求/月免費額度
- **移植而非引用**：評分邏輯和擷取邏輯是「複製移植」到 `web/backend/` 的獨立檔案，不是 import 原始腳本。原因：(1) 不修改原始專案檔案的約束 (2) Cloud Run 容器無法存取本機 `.agent/skills/` 路徑
- **Blaze 方案為必要條件**：Cloud Run 必須升級 Firebase 為 Blaze（按量付費），Spark 免費方案不支援。但輕量使用實際費用為零
- **BIM 評分為獨立維度**：通用工程人才評分（M/N/E/D 體系，門檻 20 分）和 BIM 主任職缺適配度（100 分制，5 維度）是兩套獨立評分，並排顯示
- **區域選擇 asia-east1**：部署在台灣區域以降低延遲
- **不動現有檔案**：所有新增內容嚴格放在 `web/` 子目錄，CLAUDE.md、scripts、references 等一律不修改

### Pending（下 session 可接續）
- **安裝 gcloud CLI**：到 https://cloud.google.com/sdk/docs/install 下載 Windows 安裝包
- **Firebase 登入 + 建專案**：用 `codefortaiwan.com@gmail.com` 執行 `firebase login` + `gcloud auth login` + `firebase projects:create hrmd-resume-checker`
- **升級 Blaze**：Firebase Console → 專案設定 → 帳單 → 升級
- **部署 Cloud Run 後端**：`gcloud run deploy hrmd-backend --source . --region asia-east1 --allow-unauthenticated --memory 1Gi --timeout 120 --max-instances 3 --min-instances 0`
- **部署 Firebase Hosting**：`firebase use hrmd-resume-checker && firebase deploy --only hosting`
- **端到端測試**：上傳真實 104 PDF 履歷驗證完整流程
- **未來擴展**：Firebase 後台蒐集使用者資訊（Firestore）

### 拒絕的選項（省得重議）
- **Cloud Functions gen2 (Python)**：部署大小限制 500MB，MarkItDown 全依賴可能超過。且 Cloud Run 給予更多控制（memory、timeout、Docker）
- **直接 import 原始腳本**：Cloud Run 容器獨立運行，無法存取本機路徑；且修改原始腳本違反 CLAUDE.md 約束
- **純前端方案（client-side）**：PDF 轉檔和評分需要 Python 後端，無法在瀏覽器端執行 MarkItDown
- **使用嵌入式 Python 3.14**：僅為本機 Windows 工具，Cloud Run 跑 Linux，需用標準 Docker image（python:3.12-slim）
- **Spark 免費方案**：不支援 Cloud Run / Cloud Functions，必須 Blaze

### 下次可接續的起手式
1. 確認 gcloud CLI 已安裝：`gcloud --version`
2. 如果已安裝，直接按順序執行部署指令（見 Pending 區塊），每步都是可複製貼上的命令
3. 如果要在其他電腦接續：`git pull` 後讀本筆 session-summary + `.claude/plans/optimized-finding-tower.md`，即可了解完整架構與待辦
4. 部署完成後，用根目錄下任意一個 PDF 履歷測試 `https://hrmd-resume-checker.web.app`

---

## 2026-04-30 Session：space-manager v0.2/v0.3 + 11 位結案 + 104 刊登優化（plan 檔）

### 本 session 完成項（已寫回專案）

#### 規則升級鏈
- **space-manager overlay v0.1 → v0.2**：依 5 位誤選回饋（陳偉恩/陳晏儀/甘家榕/沈聖峰/陳威龍）新增 D11 BIM 講師、D12 純建模、D13 純土建結構降級規則；E2/E8 條件化解禁加上「需 MEP 實質字眼」與「室內設計收緊」雙閘門
- **space-manager overlay v0.2 → v0.3**：依 11 位正式候選歸納，補強 BIM_TOKENS（Smart3D/SmartPlant/SketchUp/BIM-19650/ISO 19650）+ REGULATION_TOKENS（執照圖/申照圖/性能式審查/WELL/鉑金級/PIC/S GMP/local code）+ CROSS_SYSTEM_TOKENS（CSD/SEM、CSD&SEM、PCM&承攬商）
- **故意排除的關鍵字**：CSD（單詞）、審圖、套圖——純建模履歷會大量誤判，已驗證沈聖峰「CSD套圖-3D二次審圖」會被誤救
- **byte-for-byte 驗證**：default 與 mep-design 對 ANALYSIS.md (226 人) 輸出完全不變

#### /merge 步驟產出
- **HR_Data_Summary.csv** 9 欄 → 11 欄（新增「審閱結果建議」「審閱排除理由簡述」），utf-8-sig 編碼，11 位全數標註「正式候選」
- **historical_selections.csv**：135 → 146 筆（追加 11 筆 batch=batch_space_mgr_v0_2_20260430，role=space-manager）
- **PDF/MD 序號前綴改名**：001~011 + 吳伊証補登（檔名空格問題已修正）
- **bim_scorer.py 評分**（space-manager 100 分制）：11 位平均 72.4/100；005 朱育宏 82、011 吳伊証 81 強力推薦；其餘 8 位 60-77 建議面試

#### 跨平台修正
- `convert_pdfs.py` 與 `extract_hr_data.py` 加入 `sys.platform == 'win32'` 分流：Windows 保留原 `c:\Users\01102088\Desktop\HRMD` 路徑，macOS/Linux 從 `__file__` 推算專案根
- 已在註解標明「跨平台路徑解析（2026-04-30 新增）」

#### iteration_log.md 追加
- v0.2 落差分析條目（5 位誤選 + 連帶剔除 6 位說明）
- v0.3 結案審閱反饋條目（規則缺口分析 + 故意排除字眼說明）

### 對齊的概念
- **多角色 overlay 機制**：default / mep-design / space-manager 三條獨立規則路徑，所有 v0.2/v0.3 變更都用 flag 守門僅在 space-manager 啟用
- **N18 BIM × MEP 共現**：反「BIM 表演」核心規則，計算同一段落是否同時有 BIM 與 MEP token
- **D7 / D11 / D12 / D13 互斥**：D7 是 BIM-only 降級基底，D11 抓教學身份，D12 抓純建模比例，D13 抓純土建結構
- **保守加詞優於激進加詞**：第一次嘗試加 CSD/審圖 等廣義字眼會把純建模員救回，必須收緊只用組合詞（CSD&SEM）和具體認證（WELL 鉑金、PIC/S GMP）

### 招募哲學洞察（已存入 memory，下次 session 自動載入）
1. **外寬內嚴**：對外刊登寫得寬、證照軟化；對內篩選用規則精準擋
2. **對外文案紀律**：僅引用使用者明確說的；不從候選人履歷推斷團隊工作內容；公開演講等需 WebSearch 驗證
3. **跨平台 Python**：跟隨當前機器最新 Python；版本衝突才停下回報
4. **統一 ANALYSIS.md**：所有 role overlay 共用同一份來源檔，不建分支

### Pending（下次 session 可接續）
1. **下週看 104 配對結果**（使用者語）：刊登內容暫不調整，先觀察新一批應徵履歷品質
2. **下次新 ANALYSIS.md 進來時跑 v0.3 規則**：
   ```
   python3 .agent/skills/hr-talent-screener/scripts/pipeline_clean.py ANALYSIS.md
   python3 .agent/skills/hr-talent-screener/scripts/screen_candidates.py ANALYSIS.md --role=space-manager
   ```
3. **觀察指標**：
   - 50+ 入選比例（上次 12/226 ≈ 5.3%，目標 ≥ 8%）
   - 5 類誤選（純 BIM 講師/純建模/純土建/純室內/結構柱樑）攔截率
   - 新候選與 11 位歷史選人特徵相似度
4. **104 刊登優化建議**（plan 檔，使用者尚未決定執行）：
   - 位置：`/Users/shuotaochiang/.claude/plans/104-idempotent-backus.md`
   - 內含：職類 3 選建議（營建主管/水電工程師/工程配管繪圖）、薪資 50-60K 下限上調、出差「三個月以下」、職務說明 5→7 點改寫、擅長工具/工作技能/證照 3 空欄填寫建議
5. **11 位邀請信草稿**（plan 檔同位置）：
   - 主版本（10 位外部候選人共用，~480 字，全部用使用者 LinkedIn 兩篇文的原句）
   - 11 位個人化錨點建議（每位 1 句，附上候選人特徵）
   - **003 張益峮特殊**：他現職中鼎工程（中鼎內部員工），不適用 104 邀請信，建議走內部部門接洽

### 重要事實提醒
- **003 張益峮目前在中鼎工程任職**（CSV「近期工作」欄、PDF 檔名「003_張益峮（CTCI).pdf」皆顯示）。後續若新人接手，看到 CSV 中他的公司是「中鼎工程股份有限公司」要意識到這是內部候選，**不能用 104 主動邀請信流程**
- **沈聖峰 CSD 誤救案例**：他希望職稱「3D BIM技術顧問/Revit教學助教/快速建模土建結構塑模/機電CSD套圖-3D二次審圖」會在 CROSS_SYSTEM_TOKENS 加入「CSD」「審圖」單詞時被誤救出排除名單。所以這些單詞**故意不放進規則**
- **/handoff 不是這個專案的 skill**：要寫交接 log 直接 append 到 `log/2026-04.md`，不要試 slash command

### 拒絕的選項（省得重議）
- **D12 純建模降級用「總段數比例 ≥ 50%」算法**：第一版用「BIM token 出現次數 ≥ 3 次」會誤殺真正有 BIM 經驗但段落少的人；改為段落佔比 + 純工程實質段 ≤ 1 才觸發，更精準
- **加 4D/5D 到關鍵字**：使用者明確澄清團隊不做 4D/5D；候選人 004 張躍騰有但這是他個人經歷
- **CSD 單詞加進跨系統**：純建模履歷常見「CSD套圖」會誤救沈聖峰
- **104 職務說明寫具體工具列表（Revit/Navisworks/Smart3D）**：使用者糾正「BIM 統稱就好」
- **104 職務說明寫綠建築/WELL/PIC/S GMP**：使用者明確「我們沒有，你亂講」
- **公開引用「江碩濤 BIM Taiwan 演講」**：WebSearch 找不到具體紀錄，避免誇大

### 下次可接續的起手式
1. 接續者讀本筆 session-summary + `.claude/plans/104-idempotent-backus.md`（如果還在）
2. 讀記憶系統：`/Users/shuotaochiang/.claude/projects/-Users-shuotaochiang-Desktop-HR-LIST/memory/MEMORY.md`（5 條已自動載入）
3. 確認當前狀態：跑 `python3 .agent/skills/hr-talent-screener/scripts/screen_candidates.py ANALYSIS.md --role=space-manager`，預期 32 候選人 / 183 排除
4. 等使用者提供下一批 ANALYSIS.md 進來，再走 /filter → 觀察指標 → 必要時 /improve

